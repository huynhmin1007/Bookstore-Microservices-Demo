server:
  port: 9003

spring:
  application:
    name: book-service
  config:
    import: "optional:configserver:http://config-server:9999"

  data:
    mongodb:
      uri: mongodb://admin:root@host.docker.internal:27017/book-service?authSource=admin

app:
  security:
    public-endpoints: [ ]